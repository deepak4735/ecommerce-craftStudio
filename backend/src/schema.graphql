# import * from './generated/prisma.graphql'

type Mutation {
  createUser(
    name: String!
    email: String!
    password: String
    permissions: UserCreatepermissionsInput
  ): User!
}

type Query {
  users: [User]!
}
