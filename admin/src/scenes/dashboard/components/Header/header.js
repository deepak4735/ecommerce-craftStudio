import React from 'react';

// Import styles
import { HeaderContainer } from './styles';

const Header = () => {
  return (
    <HeaderContainer>
      <p>Header</p>
    </HeaderContainer>
  );
};

export default Header;
